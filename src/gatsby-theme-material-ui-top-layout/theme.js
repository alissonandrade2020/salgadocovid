import { createMuiTheme } from "@material-ui/core/"

const theme = createMuiTheme({
  palette: {
    primary: {
      main: "#0984e3",
    },
    secondary: {
      main: "#556cd6",
    },
    text: {
      primary: "#000",
      secondary: "#fff",
    },
    background: {
      default: "#d0e7f4",
    },
  },
})

export default theme
